import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jgreenberg.squaredcirclehelper',
  appName: 'Squared Circle Helper',
  webDir: 'dist'
};

export default config;
