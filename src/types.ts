export type TerritoryId = 'crockett1986'|'wwf1985_90'|'combined';
export type MatchType = 'singles'|'tag'|'sixManTag'|'eightManTag'|'battleRoyal'|'royalRumble'|'survivorSeries'|'survivorSeries4'|'survivorSeries5'|'tagBattleRoyal'|'bunkhouseBattleRoyal'|'warGames';
export type MatchRule = 'Standard'|'No DQ'|'Steel Cage'|'Two out of Three Falls'|'Survivor Series Elimination'|'WarGames'|'Bunkhouse Rules';
export type SimMode = 'full'|'quick';
export type MoveLetter = 'A'|'B'|'C'|'D'|'E';
export type PinMode = 'standard'|'optionB';
export type Move = { from:number; to:number; name:string; bonus:number; letter?:MoveLetter; pin?:'setup'|'auto' };
export type Wrestler = { id:string; name:string; pp:number; rank:number; territory:TerritoryId|'both'; qpr:string; style:string; brAdj:number; moves:Move[] };
export type Log = { cls?:string; html:string };
export type NotableEvent = { category:string; text:string };
export type SideState = { wrestler:Wrestler; currentPP:number; startPP:number; startLevel:number; pending?:Move|null; finishers:number; nearfalls:number; blood:boolean; controlPenalty:number };
export type TeamState = { name:string; official:boolean; finisher:string; members:[SideState,SideState]; active:0|1; breakups:number; hotTagUsed:boolean; miscueRisk:number };
export type Match = { a:SideState; b:SideState; timeLimit:number; moveLimit:number; movesUsed:number; result:string|null; winner?:string; logs:Log[]; controlSwings:number; lastControl?:string; rating?:number; drama:number; goat:boolean; pinMode:PinMode; territory:TerritoryId; officialMatch:boolean; pointsApplied?:boolean; kind?:'singles'|'tag'; rule?:MatchRule; simMode?:SimMode; tag?:{ teamA:TeamState; teamB:TeamState }; notables:NotableEvent[]; lastFinishMove?:string };;
export type CardMatch = { id:number; type:MatchType; label:string; labelMode?:'auto'|'custom'; a?:string; b?:string; teamA?:string[]; teamB?:string[]; teamAPreset?:string; teamBPreset?:string; entrants:string[]; entrantMode?:'random'|'selected'; entrantCount?:number; mode?:SimMode; rule?:MatchRule; result?:string; rating?:number; winnerId?:string; loserId?:string };
export type EventType = 'House Show'|'TV Show'|'Premium TV'|'Small PPV'|'Major PPV'|'Biggest PPV';
export type EventCard = { name:string; date:string; venue:string; eventType:EventType; territory:TerritoryId; matches:CardMatch[]; history:string[] };
export type RosterRelations = Record<string,{allies:string; enemies:string}>;
export type Feud = { id:string; a:string; b:string; heat:number; note:string; active:boolean };
export type MatchHistoryEntry = { id:string; date:string; territory:TerritoryId; eventName:string; eventType?:EventType; venue?:string; matchType:string; result:string; participants:string[]; winnerId?:string; rating?:number; notables?:string[]; eliminations?:Record<string,string[]>; entryOrder?:string[] };
export type StoredPoints = { wrestlers:Record<string,number>; teams:Record<string,number> };
export type TagTeamTemplate = { id:string; name:string; members:[string,string]; territory:TerritoryId|'both'; finisher:string; rating?:'A'|'B'|'C'|'D'; rankingPoints?:number; note?:string };
export type Manager = { id:string; name:string; territory:TerritoryId|'both'; stable:string[]; note?:string };
